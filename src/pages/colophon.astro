---
import config, { colophon } from '@/config'
import Layout from '@/layouts/Page.astro'

const { text, url } = config.footer.license
---

<Layout title="版权说明" description={text}>
  <h1>版权说明</h1>
  所有内容以 <a href={url}>{text} 协议</a> 发布。你可以在 GitHub 上找到源码。
  <ul>
    {
      colophon.map((item) => {
        const { name, url, license, usage, author } = item
        return (
          <li>
            - <a href={url}>{name}</a> —— {`${author} (${license})`}{usage && `, used for ${usage}`}
          </li>
        )
      })
    }
  </ul>
</Layout>
